#include <stdio.h>
#include <locale.h>

int main() {

  setlocale(LC_ALL, "Portuguese");

  int raio;
  int volume;

  printf("Digite o valor do raio da sua esfera: ");
  scanf("%d", &raio);

  volume = 4 / 3 * 3.14 * (raio^3);

  printf("O valor do volume da sua esfera é: %d", volume);
  
}
